# Чемоданчик Космограммы

Файл для переноса проекта между диалогами. **Читать первым**, прежде чем спрашивать про пункт плана.

**Обновлён: 13 августа 2026, 05:20.** За сутки — четыре партии и большая работа на сервере.

---

## Текущее состояние

| | |
|---|---|
| **Версия** | `1.283.0` — одинакова в `sw.js`, `GAME_VERSION` и во всех 18 тегах `?v=`. Рассинхрон, живший семь партий, закрыт |
| **В небе подтверждено** | да — телеметрия видела `1.283.0` 13.08 в 00:23 UTC |
| **Стенд** | `tests/guard.mjs` — **106 стражей**, все зелёные. Прогон ~65 с |
| **Приборов нет** | `playtest.mjs` и `eyes.mjs` **отсутствуют в репозитории**. Два верхних уровня доказательства (замер бота и снимки живой игры) недоступны — всё проверяется третьим |
| **Сервер** | `cosmogram-sync` v22, `cosmogram-beacon` **v5**, `config.score_strict='1'`, `dev_chat_id` исправлен |
| **Расписания** | `cosmogram-digest` 05:00 UTC ежедневно · `cosmogram-beacons-cleanup` 04:00 UTC первого числа |

### Что залито, а что ждёт

- **`1.283.0`** — залито владельцем, подтверждено телеметрией.
- **Партия А** (сторонние каналы вон) — передана архивом, статус заливки на 05:20 неизвестен.
- **Партия В** (гость виден) — передана архивом, серверная часть **уже развёрнута**, клиентская ждёт заливки.

Серверная часть работает независимо от клиента: `guest_days` примет дневник, как только доедет `beacon.js` с `BEACON.days()`.

---

## Что сделали четыре партии 13 августа

### 1. Версия и раздача

`GAME_VERSION` был на `1.282.24`, а `sw.js` и все `?v=` — на `1.282.21`. Кэшбастер не менялся семь партий: адрес модуля тот же, значит браузер вправе отдать старый файл. Отсюда шесть версий в небе одновременно.

`bump_version.py` переписан: знал три места, а их было четыре — подпись релиза Sentry он не видел. Появился режим `--align` (прежняя защита «стоп при расхождении» была тупиком — чинить рассинхрон скриптом было нельзя) и запрет опускать номер вниз со сравнением числами, а не строками. **Страж 96.**

### 2. Sentry и Amplitude убраны совсем

Два тега, два `preconnect`, два запала, десять вызовов `amplitude.track` по модулям, раздел в `privacy.html`. Причина замеренная: ноль событий за всё время. **Страж 104** сторожит невозврат; **67 и 79** переписаны под новую правду.

### 3. Утренняя сводка

`pg_cron` + `pg_net`, всё внутри базы, без новой edge-функции. Функции `analytics.digest_text()`, `analytics.digest_send()`, словарь `analytics.ru()`. Чистка писем раз в месяц со сводом `analytics.beacons_daily` перед удалением и замком «не сведено — не удаляю».

**Попутно найдена поломка возрастом в сто с лишним версий:** в `config.dev_chat_id` девять цифр вместо десяти. Ни одно уведомление «🆕 Новый тип» не дошло с v1.108.1. Адрес исправлен, `cosmogram-beacon` научен оставлять след `notify_fail` при неуспехе доставки.

### 4. Гость виден

`BEACON.days()` в `beacon.js`, один вызов на посадке в `ui.js`, таблица `guest_days` с триггером, раздел 3а в `privacy.html`. **Страж 105.** Ядро не тронуто.

---

## Ждёт твоего решения

1. **Призрак «ЕЩЁ РАЗ?»** — заказан, превью показано, разбор цены дан, три вопроса о поведении отвечены. **Вариант не выбран** (1 — только подпись, 2 — как просил, 3 — гасить всех) и **нет отдельного «да» на правку ядра**. Решения по длительности подписи (4 секунды или весь полёт) тоже нет.
2. **`config.score_strict`** — сейчас `'1'`. Вчера это вредило старым клиентам; сегодня раздача починена, и семеро на старых сборках при следующем открытии получат `1.283.0` вместе с паспортом. Предложено оставить как есть.
3. **Выставленные наружу схемы** — проверить в панели: Settings → API → Exposed schemas. Там должны быть только `public` и `graphql_public`. Это единственное, что сейчас держит закрытой схему `net` (исходящий HTTP), потому что отозвать права из миграций нельзя — гранты выданы ролью `supabase_admin`.

---

## Открытые долги

### Ядро — четыре про кадр (следующая работа по плану)

| Долг | Где | Цена бездействия |
|---|---|---|
| `vignetteSprite()` создаёт новый холст при каждой смене W/H/DPR/SC и не зануляет старый | `render.js:108–119` | GPU-память не отдаётся; на iOS WKWebView лимит ~384 МБ. `nebulaField()` уже вылечен |
| Интерполяции кадра нет: шаг и аккумулятор есть, `alpha = acc/STEP` не считается | `render.js:1001–1021` | микро-дёрганье на 120 Гц и после троттлинга |
| `Path2D` не используется — силуэт камня собирается `verts.forEach` с `cos/sin` каждый кадр | `render.js:724` | ~6000 пар тригонометрии в секунду |
| Контекст без `alpha:false` / `desynchronized:true` | `core.js:1371` | браузер зря смешивает непрозрачное небо со страницей |
| `imageSmoothingQuality` нигде не задан | ноль упоминаний | по умолчанию обычно `'low'`, но не гарантировано |

### Процесс и инструменты

- **`playtest.mjs` и `eyes.mjs` вернуть в репозиторий.** Без них недоступны два верхних уровня доказательства.
- **`bump_version.py` — нет режима «покажи и не пиши».** Запуск «посмотреть» уже едва не поднял версию: спасло только то, что `head` оборвал вывод и python умер до записи.
- **Пустой `catch` вокруг `notifyDev` — закрыт**, но тот же класс стоит поискать в других местах: любой `catch`, глотающий отказ доставки.
- **`index.html` обещает «второй замок — тихую проверку версии на живом сервере».** В хвосте файла её нет — ни `fetch`, ни `location.reload`. Либо не написана, либо снята без правки комментария.
- **Партия «Показывать призраков»** (`GHOSTS_VISIBLE`) была залита коммитом `71c60a2` и стёрта следующим `995ec24`. В HEAD её нет, а в Каталоге находка №32 описана как сделанная. Переделать или достать из коммита.
- **Страж 96 занял пустовавший номер** — тот, что погиб в том же откате.
- `cosmogram-mirror-sync` и `cosmogram-daily` не читались ни разу.
- Тумблеры настроек «через раз» — не воспроизведено.

---

## Дорожная карта

| # | Пункт | Статус |
|---|---|---|
| 1 | Гироскоп + откат | ✅ корень вылечен, `cal_storm` разобран по источникам |
| 2 | Переработка HUD | 🟡 голый край вне Telegram починен; сама переработка не начата |
| 3 | Финал режимов + баланс волн | 🟡 шаг ускорен на 12.5%, «Дыхание неба» с защитой открытия |
| 4 | Формула очков + цены кораблей | 🟡 тир-цены вернулись; формула не пересматривалась ни разу |
| 5 | «Поделись забегом» | ⬜ не начато |
| 6 | Вау-эффекты + полный экран | 🟡 полный экран есть; эффекты не начаты |
| 7 | Убрать обучение | ⬜ не начато |
| 8 | Релиз: песочница → тест → git | ✅ раздача починена, версия одна везде, дисциплина заливки записана законом |
| 9 | Своя аналитика без чужих SaaS | ✅ сводка, чистка, гость виден. Осталось: поток событий на первые 10 минут жизни игрока |

---

## Законы разработки

1. **Не чинить так, чтобы стало хуже.** Работающее не трогать.
2. **Ядро** — `core.js`, `game.js`, `render.js`. Не менять без явной просьбы.
3. **Разбор цены до кода.** Противоречия, цена, время, 2–3 варианта, рекомендация. Стоп до «да».
4. **Превью для визуальных правок.**
5. **Страж перед изменением системы**, и он обязан сначала покраснеть.
6. **Бэкап папки** перед новым модулем.
7. **Одна фича — полный цикл:** код → стражи ×3 → коммит → снимок.
8. **Откат при поломке** — немедленно.
9. **В GitHub — только по явному «готово для людей».**
10. **Каталог ошибок** пополняется при боевом крещении.
11. **Залито ≠ роздано ≠ работает.** Три события, три проверки. Раздача проверяется в `beacons`, не в git.
12. **Заливка целой папкой — это замена всего дерева.** Перед upload сверять `GAME_VERSION` заливаемой папки с тем, что уже в GitHub. *(Три отката за сутки 12–13.08.)*
13. **Номер версии — часть контракта с игроком и единственный кэшбастер раздачи.** Менять только `bump_version.py`.
14. **У канала уведомлений неуспех доставки обязан оставлять след.** Иначе о смерти канала узнаёшь через сам этот канал. *(`dev_chat_id`, сто с лишним версий молчания.)*
15. **Успешный `REVOKE` не означает, что право отозвано.** Проверять `has_*_privilege` после отзыва, а не отсутствие ошибки. У каждого гранта есть автор; чужой не перебить.
16. **Если инструмент печатает то, что собирается сделать, он обязан уметь только печатать.** Предпросмотр, после которого нельзя передумать, — не предпросмотр.
17. **Одна дурная строка не должна уносить честные.** Пакетная вставка падает целиком: чистить и различать надо до базы, а не надеяться на триггер.

---

## Что проверить на телефоне

1. **Версия в «Об игре» — `1.283.0`.** Свернуть, вернуться, проверить снова.
2. **Трасса дня у двоих совпадает** — главное обещание партии 8.
3. Гироскоп рулит, ноль берётся, переворот не уводит руль.
4. **Темп после партий 23, 26 и 28:** первая волна — «крючок», дальше дыхание передышка↔разгон.
5. **Станция и звезда дня переживают потерю холста** — свернуть/развернуть под нехваткой памяти.
6. **Верхний HUD вне Telegram** — открыть в обычном браузере, счёт не должен прижиматься к краю.
7. **Музыка не заучивается** — послушать пару минут.
8. **После заливки партии А:** страница не ходит на чужие CDN; `privacy.html` читается связно.
9. **После заливки партии В:** сыграть **без входа** (обычный браузер, не входя) — через минуту в `guest_days` должна появиться строка с сегодняшним днём.
10. **Выключить тумблер отчётов и сыграть гостем** — в `guest_days` не должно появиться ничего.

---

## Копилка идей

- **«Летопись»** — архив прошедших Трасс дня: ключ вычисляется из даты, небо любого прошлого дня доступно без сервера и без байта хранения.
- **«Зерно»** — показать игроку сид его полёта как памятник.
- **Щадящее обнуление серии** (7→3, приём Duolingo).
- **`requestWriteAccess`** — без него бот не может написать игроку ни «Трасса дня готова», ни «твой рекорд побит». Метод уже есть в мосте, код его не зовёт. Самый весомый рычаг возврата.
- **`addToHomeScreen`**, **`requestEmojiStatusAccess`**, **`switchInlineQuery`**, **`DeviceStorage`** — то же самое: есть в мосте, не используются.
- **Поток событий на первые 10 минут жизни игрока** — вторая половина работы по аналитике, отложена сознательно.
- **«Кости» из NetHack** — гибель оставляет след, который находит другой; ложится на призраков.
- **Целевые метрики:** возврат на 2-й день > 30%, на 7-й > 10%, сессия > 3 мин, падений < 0.5%, холодный старт < 3 с.

---

## И главное, чего не решает ни одна партия

На 13 августа в игре пятнадцать зарегистрированных человек, и за минувшие сутки в небе был один — владелец. Аналитика теперь покажет это точнее, но не изменит. Разговор о том, откуда брать игроков, — отдельный, и он важнее всего, что сделано за эти сутки.
