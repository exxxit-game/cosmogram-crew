// v1.280.0 «Забытый щуп закрыт»: это был разовый диагностический пробник — читал bot_token
// из config, стучался в Telegram getMe, возвращал публичные данные бота (не сам токен).
// Риск был низкий в обе стороны, но лишняя открытая дверь без явной цели — не повод её держать.
// Полное удаление функции требует панели Supabase (нет инструмента для этого здесь) — до тех
// пор эта версия отвечает всем запросам одинаково честно: щупа здесь больше нет, в config не лезет,
// к Telegram не стучится.
Deno.serve((_req) => {
  return new Response('Этот пробник отключён.', {
    status: 410,
    headers: { 'content-type': 'text/plain; charset=utf-8' },
  });
});
